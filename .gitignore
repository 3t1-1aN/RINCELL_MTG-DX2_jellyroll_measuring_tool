.env
# Desktop OAuth client JSON is shipped in the Windows release zip for operators.
# Keep token.json local-only; never package user auth tokens.
token.json
__pycache__/
*.pyc
.venv/
battery_master_log.csv
build/
dist/
release/
*.spec.bak
.gstack/
